
extends KinematicBody2D

var run_speed = 25 //runspeeed
var velocity = Vector2.ZERO //the movement vector
var player = null //reference to player
var m=0;  //distance variable



func _physics_process(delta):
 velocity = Vector2.ZERO //catch in movement velocity
 if player:
 velocity = position.direction_to(player.position) * run_speed
 velocity = move_and_slide(velocity) //movement to the player

func _on_DetectRadius_body_entered(body): 
//help detect the player so not all enemy converge at once
 player = body

func _on_DetectRadius_body_exited(body):
//help detect the player when he leaves
 player = null


func _process(delta)
m=position.distanceto(player.position);
if(m<=4)
//put attack function here

////////////////////////////////////////////////
NOTE :1. the func process which i beleive is the update
 method can all also be run in func physics method
in the position marjet insert script
//////////////////////////////////
2. the script works on the assumption there will
 be only one player type
////////////////////////////////////////////